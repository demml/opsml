name: opsml-name
space: opsml-space

service:
  version: 1.0.0
  write_dir: opsml_app/test_service
  cards:
    - alias: model
      space: model-space
      name: model-name
      version: 1
      type: model
      drift:
        active: true
        deactivate_others: false
        drift_type: [psi]
